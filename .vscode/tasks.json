{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "controlplane ignition",
      "type": "shell",
      "command": "butane -p -o controlplane.ign controlplane.yaml",
      "options": {
        "cwd": "${workspaceFolder}/microos/ignition"
      }
    },
    {
      "label": "worker ignition",
      "type": "shell",
      "command": "butane -p -o worker.ign worker.yaml",
      "options": {
        "cwd": "${workspaceFolder}/microos/ignition"
      }
    }
  ]
}