{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "generate ignition",
      "type": "shell",
      "command": "butane -p -o controlplane.ign controlplane.yaml && butane -p -o worker.ign worker.yaml",
      "options": {
        "cwd": "${workspaceFolder}/microos/ignition"
      }
    }
  ]
}